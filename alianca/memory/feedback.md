# feedback — aprendizado do desenvolvimento da Aliança

> Regras curtas vindas de correção do usuário ou erro recorrente. Consulta obrigatória em `code-quality`/`health-check`. Formato: `- [AAAA-MM-DD] Sintoma/correção → regra.`

- [2026-07-03] A esteira de testes no onboarding **falhava** (bootstrap dado como "pronto" sem testes rodando). Regra: bootstrap (`setup`/`adopt`) só fecha com a esteira **armada e verde** e `alianca/kernel/verify.cmd` apontando pro comando de teste — em andamento monta a esteira **completa já**; novo **entende primeiro**, depois monta. Sem `verify.cmd` o 3º portão (`verify.py`) fica inerte. Ver `testing` §Esteira desde o onboarding.
- [2026-06-07] O usuário escreve e é respondido em **PT-BR**. Nomes de produto/arquivo: **simples e comerciais**, nunca siglas técnicas (ele rejeitou "APBP" por seco/técnico). Padrão adotado: `START-HERE.md`, `router.md`, `instructions/`, `memory/`, `snapshots/`. Edições comerciais: Start (P0/P1) · Pro (P2) · Org (P3).
- [2026-07-02] A memória nativa do Claude Code (*auto memory*) **reenche sozinha a cada sessão** e compete com `alianca/memory/` → é a fonte da alucinação de longo prazo (o modelo recita estado velho em vez de reler o arquivo real). Regra: em Claude Code, **desativar** a auto memory (`"autoMemoryEnabled": false` no `.claude/settings.json` versionado), não apenas apontar. Fonte única = `alianca/memory/`. Ver `adopt` §Regra Zero.
- [2026-06-08] **A Aliança é arquiteta/gerente, não construtora.** Ela diz QUAIS ferramentas e COMO fazer — não entrega a casa pronta (nada de scaffolding/template detalhado nem fazer o trabalho pelo dev). **"Menos é mais":** ter menos peças, cada uma completa e suficiente pra "compor a música", sem instrumento redundante. Antes de criar módulo/molde/seção nova, checar se um **invariante curto + gatilho** (ou um ponteiro) já resolve; caçar duplicação e colisão de nome; na dúvida, cortar. (Foi por isso que `stacks/` saiu na 2.1.)
